// ❌ STALE TODOs — undated, unowned, unactionable; nobody knows if still valid.
export const config = {
  timeout: 5000,   // TODO: make configurable (added long ago)
  retries: 3,      // FIXME: this breaks sometimes??
  region: "us",    // HACK: hardcoded, fix later
  // TODO: add caching
  // TODO: support multiple regions
  // XXX: remove before launch (launched 2 years ago)
};
