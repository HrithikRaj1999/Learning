# Comments — Dead Code — Fix Hints
> Git is your history; the file is not a graveyard.
## Wrong now
Old v1 logic sits commented out "just in case", plus a vague revert TODO. Noise
that rots and confuses.
## Hints
- [ ] Delete the commented-out blocks entirely — they live in version control.
- [ ] If a decision matters, record WHY in a short comment or commit message, not
      dead code.
- [ ] Replace the vague TODO with an actionable, tracked one or remove it.
## Done-when
- [ ] Zero commented-out code; only the live implementation remains.
