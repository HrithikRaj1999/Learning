# React Scenario Guide - Real-Life Expectations

Each React entry explains the user-facing situation behind the question, what usually breaks, what to build, and how to test it.

## Challenge 01.01 - 01 - Component Patterns & JSX Mastery
- File/folder: `React/01_Component_Patterns_JSX`
- Function/code to work on: `Build a Layout component with header/sidebar/content slots`
- Original ask: Build a Layout component with header/sidebar/content slots. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a Layout component with header/sidebar/content slots. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a Layout component with header/sidebar/content slots. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 01.02 - 01 - Component Patterns & JSX Mastery
- File/folder: `React/01_Component_Patterns_JSX`
- Function/code to work on: `Implement a conditional rendering utility (Show/Hide component)`
- Original ask: Implement a conditional rendering utility (Show/Hide component). Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement a conditional rendering utility (Show/Hide component). Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement a conditional rendering utility (Show/Hide component). Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 01.03 - 01 - Component Patterns & JSX Mastery
- File/folder: `React/01_Component_Patterns_JSX`
- Function/code to work on: `Create a polymorphic `<Box as="section">` component`
- Original ask: Create a polymorphic `<Box as="section">` component. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Create a polymorphic `<Box as="section">` component. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Create a polymorphic `<Box as="section">` component. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 01.04 - 01 - Component Patterns & JSX Mastery
- File/folder: `React/01_Component_Patterns_JSX`
- Function/code to work on: `Build a List component that handles empty/loading/error states`
- Original ask: Build a List component that handles empty/loading/error states. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a List component that handles empty/loading/error states. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a List component that handles empty/loading/error states. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 01.05 - 01 - Component Patterns & JSX Mastery
- File/folder: `React/01_Component_Patterns_JSX`
- Function/code to work on: `Implement a Card component with compound sub-components`
- Original ask: Implement a Card component with compound sub-components. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement a Card component with compound sub-components. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement a Card component with compound sub-components. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 02.01 - 02 - State Management & Hooks Deep Dive
- File/folder: `React/02_State_Hooks_Deep_Dive`
- Function/code to work on: `Implement a useReducer-based shopping cart`
- Original ask: Implement a useReducer-based shopping cart. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement a useReducer-based shopping cart. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement a useReducer-based shopping cart. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 02.02 - 02 - State Management & Hooks Deep Dive
- File/folder: `React/02_State_Hooks_Deep_Dive`
- Function/code to work on: `Fix a stale closure bug in a timer component`
- Original ask: Fix a stale closure bug in a timer component. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Fix a stale closure bug in a timer component. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Fix a stale closure bug in a timer component. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 02.03 - 02 - State Management & Hooks Deep Dive
- File/folder: `React/02_State_Hooks_Deep_Dive`
- Function/code to work on: `Build a counter with undo/redo using state history`
- Original ask: Build a counter with undo/redo using state history. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a counter with undo/redo using state history. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a counter with undo/redo using state history. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 02.04 - 02 - State Management & Hooks Deep Dive
- File/folder: `React/02_State_Hooks_Deep_Dive`
- Function/code to work on: `Create a form with useReducer handling all field states`
- Original ask: Create a form with useReducer handling all field states. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Create a form with useReducer handling all field states. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Create a form with useReducer handling all field states. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 02.05 - 02 - State Management & Hooks Deep Dive
- File/folder: `React/02_State_Hooks_Deep_Dive`
- Function/code to work on: `Implement useState from scratch (simplified)`
- Original ask: Implement useState from scratch (simplified). Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement useState from scratch (simplified). Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement useState from scratch (simplified). Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 03.01 - 03 - useEffect & Lifecycle Patterns
- File/folder: `React/03_useEffect_Lifecycle`
- Function/code to work on: `Fix infinite re-render loop in useEffect`
- Original ask: Fix infinite re-render loop in useEffect. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Fix infinite re-render loop in useEffect. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Fix infinite re-render loop in useEffect. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 03.02 - 03 - useEffect & Lifecycle Patterns
- File/folder: `React/03_useEffect_Lifecycle`
- Function/code to work on: `Implement data fetching with race condition handling`
- Original ask: Implement data fetching with race condition handling. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement data fetching with race condition handling. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement data fetching with race condition handling. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 03.03 - 03 - useEffect & Lifecycle Patterns
- File/folder: `React/03_useEffect_Lifecycle`
- Function/code to work on: `Build a resize/scroll listener with proper cleanup`
- Original ask: Build a resize/scroll listener with proper cleanup. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a resize/scroll listener with proper cleanup. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a resize/scroll listener with proper cleanup. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 03.04 - 03 - useEffect & Lifecycle Patterns
- File/folder: `React/03_useEffect_Lifecycle`
- Function/code to work on: `Create a debounced search with AbortController`
- Original ask: Create a debounced search with AbortController. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Create a debounced search with AbortController. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Create a debounced search with AbortController. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 03.05 - 03 - useEffect & Lifecycle Patterns
- File/folder: `React/03_useEffect_Lifecycle`
- Function/code to work on: `Explain why this effect runs twice in StrictMode`
- Original ask: Explain why this effect runs twice in StrictMode. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Explain why this effect runs twice in StrictMode. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Explain why this effect runs twice in StrictMode. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.01 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `useDebounce(value, delay)``
- Original ask: Build `useDebounce(value, delay)`. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `useDebounce(value, delay)`. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `useDebounce(value, delay)`. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.02 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `useLocalStorage(key, initialValue)``
- Original ask: Build `useLocalStorage(key, initialValue)`. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `useLocalStorage(key, initialValue)`. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `useLocalStorage(key, initialValue)`. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.03 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `useFetch(url)` with loading/error/data`
- Original ask: Build `useFetch(url)` with loading/error/data. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `useFetch(url)` with loading/error/data. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `useFetch(url)` with loading/error/data. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.04 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `usePrevious(value)``
- Original ask: Build `usePrevious(value)`. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `usePrevious(value)`. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `usePrevious(value)`. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.05 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `useIntersectionObserver` for lazy loading`
- Original ask: Build `useIntersectionObserver` for lazy loading. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `useIntersectionObserver` for lazy loading. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `useIntersectionObserver` for lazy loading. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.06 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `useThrottle(callback, delay)``
- Original ask: Build `useThrottle(callback, delay)`. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `useThrottle(callback, delay)`. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `useThrottle(callback, delay)`. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.07 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `useClickOutside(ref, handler)``
- Original ask: Build `useClickOutside(ref, handler)`. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `useClickOutside(ref, handler)`. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `useClickOutside(ref, handler)`. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 04.08 - 04 - Custom Hooks & Logic Extraction
- File/folder: `React/04_Custom_Hooks`
- Function/code to work on: `Build `useMediaQuery(query)``
- Original ask: Build `useMediaQuery(query)`. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build `useMediaQuery(query)`. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build `useMediaQuery(query)`. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 05.01 - 05 - Performance Optimization
- File/folder: `React/05_Performance_Optimization`
- Function/code to work on: `Optimize a list that re-renders all items on parent state change`
- Original ask: Optimize a list that re-renders all items on parent state change. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Optimize a list that re-renders all items on parent state change. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Optimize a list that re-renders all items on parent state change. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 05.02 - 05 - Performance Optimization
- File/folder: `React/05_Performance_Optimization`
- Function/code to work on: `Implement virtualized list rendering (only visible items)`
- Original ask: Implement virtualized list rendering (only visible items). Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement virtualized list rendering (only visible items). Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement virtualized list rendering (only visible items). Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 05.03 - 05 - Performance Optimization
- File/folder: `React/05_Performance_Optimization`
- Function/code to work on: `Fix unnecessary re-renders in a Context consumer`
- Original ask: Fix unnecessary re-renders in a Context consumer. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Fix unnecessary re-renders in a Context consumer. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Fix unnecessary re-renders in a Context consumer. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 05.04 - 05 - Performance Optimization
- File/folder: `React/05_Performance_Optimization`
- Function/code to work on: `Profile and fix a slow component tree`
- Original ask: Profile and fix a slow component tree. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Profile and fix a slow component tree. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Profile and fix a slow component tree. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 05.05 - 05 - Performance Optimization
- File/folder: `React/05_Performance_Optimization`
- Function/code to work on: `Implement windowing for 10,000 rows without libraries`
- Original ask: Implement windowing for 10,000 rows without libraries. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement windowing for 10,000 rows without libraries. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement windowing for 10,000 rows without libraries. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 05.06 - 05 - Performance Optimization
- File/folder: `React/05_Performance_Optimization`
- Function/code to work on: `When does React.memo hurt performance?`
- Original ask: When does React.memo hurt performance?. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: When does React.memo hurt performance?. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: When does React.memo hurt performance?. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 06.01 - 06 - Context API & State Architecture
- File/folder: `React/06_Context_State_Architecture`
- Function/code to work on: `Build a theme toggle with Context (dark/light mode)`
- Original ask: Build a theme toggle with Context (dark/light mode). Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a theme toggle with Context (dark/light mode). Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a theme toggle with Context (dark/light mode). Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 06.02 - 06 - Context API & State Architecture
- File/folder: `React/06_Context_State_Architecture`
- Function/code to work on: `Fix re-render issue: all consumers re-render on any context change`
- Original ask: Fix re-render issue: all consumers re-render on any context change. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Fix re-render issue: all consumers re-render on any context change. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Fix re-render issue: all consumers re-render on any context change. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 06.03 - 06 - Context API & State Architecture
- File/folder: `React/06_Context_State_Architecture`
- Function/code to work on: `Implement a multi-step form wizard with shared state`
- Original ask: Implement a multi-step form wizard with shared state. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement a multi-step form wizard with shared state. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement a multi-step form wizard with shared state. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 06.04 - 06 - Context API & State Architecture
- File/folder: `React/06_Context_State_Architecture`
- Function/code to work on: `Design auth context with login/logout/loading states`
- Original ask: Design auth context with login/logout/loading states. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Design auth context with login/logout/loading states. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Design auth context with login/logout/loading states. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 06.05 - 06 - Context API & State Architecture
- File/folder: `React/06_Context_State_Architecture`
- Function/code to work on: `Compare Context vs Zustand for a given scenario`
- Original ask: Compare Context vs Zustand for a given scenario. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Compare Context vs Zustand for a given scenario. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Compare Context vs Zustand for a given scenario. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 06.06 - 06 - Context API & State Architecture
- File/folder: `React/06_Context_State_Architecture`
- Function/code to work on: `Implement a state machine for a multi-state UI`
- Original ask: Implement a state machine for a multi-state UI. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement a state machine for a multi-state UI. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement a state machine for a multi-state UI. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 07.01 - 07 - React 18 Concurrent Features
- File/folder: `React/07_React18_Concurrent`
- Function/code to work on: `Use useTransition to keep UI responsive during expensive filter`
- Original ask: Use useTransition to keep UI responsive during expensive filter. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Use useTransition to keep UI responsive during expensive filter. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Use useTransition to keep UI responsive during expensive filter. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 07.02 - 07 - React 18 Concurrent Features
- File/folder: `React/07_React18_Concurrent`
- Function/code to work on: `Implement search with useDeferredValue (instant input, deferred results)`
- Original ask: Implement search with useDeferredValue (instant input, deferred results). Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement search with useDeferredValue (instant input, deferred results). Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement search with useDeferredValue (instant input, deferred results). Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 07.03 - 07 - React 18 Concurrent Features
- File/folder: `React/07_React18_Concurrent`
- Function/code to work on: `Build a Suspense boundary with fallback and error boundary`
- Original ask: Build a Suspense boundary with fallback and error boundary. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a Suspense boundary with fallback and error boundary. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a Suspense boundary with fallback and error boundary. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 07.04 - 07 - React 18 Concurrent Features
- File/folder: `React/07_React18_Concurrent`
- Function/code to work on: `Explain automatic batching - what changed from React 17?`
- Original ask: Explain automatic batching - what changed from React 17?. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Explain automatic batching - what changed from React 17?. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Explain automatic batching - what changed from React 17?. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 07.05 - 07 - React 18 Concurrent Features
- File/folder: `React/07_React18_Concurrent`
- Function/code to work on: `When would you use useTransition vs useDeferredValue?`
- Original ask: When would you use useTransition vs useDeferredValue?. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: When would you use useTransition vs useDeferredValue?. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: When would you use useTransition vs useDeferredValue?. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 07.06 - 07 - React 18 Concurrent Features
- File/folder: `React/07_React18_Concurrent`
- Function/code to work on: `Implement streaming SSR concept with Suspense`
- Original ask: Implement streaming SSR concept with Suspense. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement streaming SSR concept with Suspense. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement streaming SSR concept with Suspense. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 08.01 - 08 - React 19 New APIs
- File/folder: `React/08_React19_New_APIs`
- Function/code to work on: `Explain Server Components vs Client Components`
- Original ask: Explain Server Components vs Client Components. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Explain Server Components vs Client Components. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Explain Server Components vs Client Components. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 08.02 - 08 - React 19 New APIs
- File/folder: `React/08_React19_New_APIs`
- Function/code to work on: `Build a form with useFormStatus showing pending state`
- Original ask: Build a form with useFormStatus showing pending state. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a form with useFormStatus showing pending state. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a form with useFormStatus showing pending state. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 08.03 - 08 - React 19 New APIs
- File/folder: `React/08_React19_New_APIs`
- Function/code to work on: `Implement optimistic UI with useOptimistic`
- Original ask: Implement optimistic UI with useOptimistic. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement optimistic UI with useOptimistic. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement optimistic UI with useOptimistic. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 08.04 - 08 - React 19 New APIs
- File/folder: `React/08_React19_New_APIs`
- Function/code to work on: `How does `use()` differ from useEffect for data fetching?`
- Original ask: How does `use()` differ from useEffect for data fetching?. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: How does `use()` differ from useEffect for data fetching?. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: How does `use()` differ from useEffect for data fetching?. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 08.05 - 08 - React 19 New APIs
- File/folder: `React/08_React19_New_APIs`
- Function/code to work on: `When should you use "use server" vs "use client"?`
- Original ask: When should you use "use server" vs "use client"?. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: When should you use "use server" vs "use client"?. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: When should you use "use server" vs "use client"?. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 08.06 - 08 - React 19 New APIs
- File/folder: `React/08_React19_New_APIs`
- Function/code to work on: `Build a like button with optimistic update + server action`
- Original ask: Build a like button with optimistic update + server action. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a like button with optimistic update + server action. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a like button with optimistic update + server action. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 09.01 - 09 - Routing & Navigation Patterns
- File/folder: `React/09_Routing_Navigation`
- Function/code to work on: `Implement protected routes with auth redirect`
- Original ask: Implement protected routes with auth redirect. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement protected routes with auth redirect. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement protected routes with auth redirect. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 09.02 - 09 - Routing & Navigation Patterns
- File/folder: `React/09_Routing_Navigation`
- Function/code to work on: `Build nested routes with shared layout`
- Original ask: Build nested routes with shared layout. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build nested routes with shared layout. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build nested routes with shared layout. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 09.03 - 09 - Routing & Navigation Patterns
- File/folder: `React/09_Routing_Navigation`
- Function/code to work on: `Sync filter state with URL search params`
- Original ask: Sync filter state with URL search params. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Sync filter state with URL search params. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Sync filter state with URL search params. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 09.04 - 09 - Routing & Navigation Patterns
- File/folder: `React/09_Routing_Navigation`
- Function/code to work on: `Implement breadcrumbs from route hierarchy`
- Original ask: Implement breadcrumbs from route hierarchy. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement breadcrumbs from route hierarchy. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement breadcrumbs from route hierarchy. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 09.05 - 09 - Routing & Navigation Patterns
- File/folder: `React/09_Routing_Navigation`
- Function/code to work on: `Build a route-based code splitting setup`
- Original ask: Build a route-based code splitting setup. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a route-based code splitting setup. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a route-based code splitting setup. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 09.06 - 09 - Routing & Navigation Patterns
- File/folder: `React/09_Routing_Navigation`
- Function/code to work on: `Handle 404 and error routes gracefully`
- Original ask: Handle 404 and error routes gracefully. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Handle 404 and error routes gracefully. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Handle 404 and error routes gracefully. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 10.01 - 10 - Forms, Validation & Controlled Inputs
- File/folder: `React/10_Forms_Validation`
- Function/code to work on: `Build a multi-step form with validation between steps`
- Original ask: Build a multi-step form with validation between steps. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a multi-step form with validation between steps. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a multi-step form with validation between steps. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 10.02 - 10 - Forms, Validation & Controlled Inputs
- File/folder: `React/10_Forms_Validation`
- Function/code to work on: `Implement a dynamic form from JSON schema`
- Original ask: Implement a dynamic form from JSON schema. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement a dynamic form from JSON schema. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement a dynamic form from JSON schema. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 10.03 - 10 - Forms, Validation & Controlled Inputs
- File/folder: `React/10_Forms_Validation`
- Function/code to work on: `Build a debounced search input that cancels stale requests`
- Original ask: Build a debounced search input that cancels stale requests. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a debounced search input that cancels stale requests. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a debounced search input that cancels stale requests. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 10.04 - 10 - Forms, Validation & Controlled Inputs
- File/folder: `React/10_Forms_Validation`
- Function/code to work on: `Implement form with real-time field validation (email, phone)`
- Original ask: Implement form with real-time field validation (email, phone). Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement form with real-time field validation (email, phone). Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement form with real-time field validation (email, phone). Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 10.05 - 10 - Forms, Validation & Controlled Inputs
- File/folder: `React/10_Forms_Validation`
- Function/code to work on: `Build an OTP input (auto-focus, paste, backspace)`
- Original ask: Build an OTP input (auto-focus, paste, backspace). Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build an OTP input (auto-focus, paste, backspace). Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build an OTP input (auto-focus, paste, backspace). Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 10.06 - 10 - Forms, Validation & Controlled Inputs
- File/folder: `React/10_Forms_Validation`
- Function/code to work on: `Controlled input performance - when to use uncontrolled`
- Original ask: Controlled input performance - when to use uncontrolled. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Controlled input performance - when to use uncontrolled. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Controlled input performance - when to use uncontrolled. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 11.01 - 11 - Data Fetching & Caching
- File/folder: `React/11_Data_Fetching_Caching`
- Function/code to work on: `Implement infinite scroll with intersection observer`
- Original ask: Implement infinite scroll with intersection observer. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement infinite scroll with intersection observer. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement infinite scroll with intersection observer. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 11.02 - 11 - Data Fetching & Caching
- File/folder: `React/11_Data_Fetching_Caching`
- Function/code to work on: `Build a useFetch hook with caching (simplified SWR)`
- Original ask: Build a useFetch hook with caching (simplified SWR). Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a useFetch hook with caching (simplified SWR). Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a useFetch hook with caching (simplified SWR). Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 11.03 - 11 - Data Fetching & Caching
- File/folder: `React/11_Data_Fetching_Caching`
- Function/code to work on: `Implement optimistic update for a like button`
- Original ask: Implement optimistic update for a like button. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement optimistic update for a like button. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement optimistic update for a like button. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 11.04 - 11 - Data Fetching & Caching
- File/folder: `React/11_Data_Fetching_Caching`
- Function/code to work on: `Handle race conditions in parallel fetches`
- Original ask: Handle race conditions in parallel fetches. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Handle race conditions in parallel fetches. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Handle race conditions in parallel fetches. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 11.05 - 11 - Data Fetching & Caching
- File/folder: `React/11_Data_Fetching_Caching`
- Function/code to work on: `Build pagination with prefetching next page`
- Original ask: Build pagination with prefetching next page. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build pagination with prefetching next page. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build pagination with prefetching next page. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 11.06 - 11 - Data Fetching & Caching
- File/folder: `React/11_Data_Fetching_Caching`
- Function/code to work on: `Implement retry logic with exponential backoff`
- Original ask: Implement retry logic with exponential backoff. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement retry logic with exponential backoff. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement retry logic with exponential backoff. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 12.01 - 12 - Testing React Applications
- File/folder: `React/12_Testing_React`
- Function/code to work on: `Test a form submission with validation errors`
- Original ask: Test a form submission with validation errors. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Test a form submission with validation errors. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Test a form submission with validation errors. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 12.02 - 12 - Testing React Applications
- File/folder: `React/12_Testing_React`
- Function/code to work on: `Test async data fetching with loading/error states`
- Original ask: Test async data fetching with loading/error states. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Test async data fetching with loading/error states. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Test async data fetching with loading/error states. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 12.03 - 12 - Testing React Applications
- File/folder: `React/12_Testing_React`
- Function/code to work on: `Mock an API call and verify correct rendering`
- Original ask: Mock an API call and verify correct rendering. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Mock an API call and verify correct rendering. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Mock an API call and verify correct rendering. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 12.04 - 12 - Testing React Applications
- File/folder: `React/12_Testing_React`
- Function/code to work on: `Test a custom hook with renderHook`
- Original ask: Test a custom hook with renderHook. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Test a custom hook with renderHook. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Test a custom hook with renderHook. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 12.05 - 12 - Testing React Applications
- File/folder: `React/12_Testing_React`
- Function/code to work on: `Write integration test for a multi-step wizard`
- Original ask: Write integration test for a multi-step wizard. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Write integration test for a multi-step wizard. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Write integration test for a multi-step wizard. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 12.06 - 12 - Testing React Applications
- File/folder: `React/12_Testing_React`
- Function/code to work on: `Test accessibility: screen reader, keyboard nav`
- Original ask: Test accessibility: screen reader, keyboard nav. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Test accessibility: screen reader, keyboard nav. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Test accessibility: screen reader, keyboard nav. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 13.01 - 13 - Component Design Systems
- File/folder: `React/13_Component_Design_Systems`
- Function/code to work on: `Build a compound Select/Dropdown component`
- Original ask: Build a compound Select/Dropdown component. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a compound Select/Dropdown component. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a compound Select/Dropdown component. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 13.02 - 13 - Component Design Systems
- File/folder: `React/13_Component_Design_Systems`
- Function/code to work on: `Create an accessible Modal with focus trap`
- Original ask: Create an accessible Modal with focus trap. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Create an accessible Modal with focus trap. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Create an accessible Modal with focus trap. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 13.03 - 13 - Component Design Systems
- File/folder: `React/13_Component_Design_Systems`
- Function/code to work on: `Build headless Tabs (logic only, no styling)`
- Original ask: Build headless Tabs (logic only, no styling). Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build headless Tabs (logic only, no styling). Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build headless Tabs (logic only, no styling). Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 13.04 - 13 - Component Design Systems
- File/folder: `React/13_Component_Design_Systems`
- Function/code to work on: `Implement a polymorphic Button (as="a", as="button")`
- Original ask: Implement a polymorphic Button (as="a", as="button"). Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement a polymorphic Button (as="a", as="button"). Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement a polymorphic Button (as="a", as="button"). Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 13.05 - 13 - Component Design Systems
- File/folder: `React/13_Component_Design_Systems`
- Function/code to work on: `Design a Toast system with provider pattern`
- Original ask: Design a Toast system with provider pattern. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Design a Toast system with provider pattern. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Design a Toast system with provider pattern. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 13.06 - 13 - Component Design Systems
- File/folder: `React/13_Component_Design_Systems`
- Function/code to work on: `Build an Accordion that supports single/multi expand`
- Original ask: Build an Accordion that supports single/multi expand. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build an Accordion that supports single/multi expand. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build an Accordion that supports single/multi expand. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.01 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Autocomplete/Typeahead`
- Original ask: Autocomplete/Typeahead. Key behavior: Debounce, API, keyboard nav, highlight. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Autocomplete/Typeahead. Key behavior: Debounce, API, keyboard nav, highlight. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Autocomplete/Typeahead. Key behavior: Debounce, API, keyboard nav, highlight. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.02 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Modal/Dialog`
- Original ask: Modal/Dialog. Key behavior: Portal, focus trap, escape, overlay. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Modal/Dialog. Key behavior: Portal, focus trap, escape, overlay. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Modal/Dialog. Key behavior: Portal, focus trap, escape, overlay. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.03 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Tabs`
- Original ask: Tabs. Key behavior: Active state, lazy render, keyboard. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Tabs. Key behavior: Active state, lazy render, keyboard. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Tabs. Key behavior: Active state, lazy render, keyboard. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.04 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Accordion`
- Original ask: Accordion. Key behavior: Single/multi expand, animation. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Accordion. Key behavior: Single/multi expand, animation. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Accordion. Key behavior: Single/multi expand, animation. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.05 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Star Rating`
- Original ask: Star Rating. Key behavior: Hover preview, click, half stars. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Star Rating. Key behavior: Hover preview, click, half stars. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Star Rating. Key behavior: Hover preview, click, half stars. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.06 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Multi-select Dropdown`
- Original ask: Multi-select Dropdown. Key behavior: Search, chips, select all, keyboard. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Multi-select Dropdown. Key behavior: Search, chips, select all, keyboard. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Multi-select Dropdown. Key behavior: Search, chips, select all, keyboard. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.07 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Toast/Notifications`
- Original ask: Toast/Notifications. Key behavior: Auto-dismiss, queue, stacking. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Toast/Notifications. Key behavior: Auto-dismiss, queue, stacking. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Toast/Notifications. Key behavior: Auto-dismiss, queue, stacking. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.08 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Tooltip/Popover`
- Original ask: Tooltip/Popover. Key behavior: Positioning, delay, hover intent. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Tooltip/Popover. Key behavior: Positioning, delay, hover intent. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Tooltip/Popover. Key behavior: Positioning, delay, hover intent. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.09 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Date Picker`
- Original ask: Date Picker. Key behavior: Calendar grid, navigation, selection. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Date Picker. Key behavior: Calendar grid, navigation, selection. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Date Picker. Key behavior: Calendar grid, navigation, selection. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.10 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Carousel/Slider`
- Original ask: Carousel/Slider. Key behavior: Auto-play, infinite loop, swipe. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Carousel/Slider. Key behavior: Auto-play, infinite loop, swipe. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Carousel/Slider. Key behavior: Auto-play, infinite loop, swipe. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.11 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Progress Bar`
- Original ask: Progress Bar. Key behavior: Animated, determinate/indeterminate. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Progress Bar. Key behavior: Animated, determinate/indeterminate. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Progress Bar. Key behavior: Animated, determinate/indeterminate. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.12 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Breadcrumbs`
- Original ask: Breadcrumbs. Key behavior: Route-based, truncation. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Breadcrumbs. Key behavior: Route-based, truncation. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Breadcrumbs. Key behavior: Route-based, truncation. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.13 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Tag/Chip Input`
- Original ask: Tag/Chip Input. Key behavior: Add, remove, paste multiple. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Tag/Chip Input. Key behavior: Add, remove, paste multiple. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Tag/Chip Input. Key behavior: Add, remove, paste multiple. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.14 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Color Picker`
- Original ask: Color Picker. Key behavior: HSL/RGB, gradient, eyedropper. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Color Picker. Key behavior: HSL/RGB, gradient, eyedropper. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Color Picker. Key behavior: HSL/RGB, gradient, eyedropper. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.15 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Skeleton Loader`
- Original ask: Skeleton Loader. Key behavior: Shimmer animation, layout matching. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Skeleton Loader. Key behavior: Shimmer animation, layout matching. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Skeleton Loader. Key behavior: Shimmer animation, layout matching. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.16 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Image Lazy Loader`
- Original ask: Image Lazy Loader. Key behavior: Intersection observer, placeholder. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Image Lazy Loader. Key behavior: Intersection observer, placeholder. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Image Lazy Loader. Key behavior: Intersection observer, placeholder. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.17 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Draggable List`
- Original ask: Draggable List. Key behavior: Drag events, reorder, drop zones. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Draggable List. Key behavior: Drag events, reorder, drop zones. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Draggable List. Key behavior: Drag events, reorder, drop zones. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.18 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Context Menu`
- Original ask: Context Menu. Key behavior: Right-click, positioning, sub-menus. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Context Menu. Key behavior: Right-click, positioning, sub-menus. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Context Menu. Key behavior: Right-click, positioning, sub-menus. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.19 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Toggle/Switch`
- Original ask: Toggle/Switch. Key behavior: Accessible, animated, controlled. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Toggle/Switch. Key behavior: Accessible, animated, controlled. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Toggle/Switch. Key behavior: Accessible, animated, controlled. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 14.20 - 14 - Machine Coding Round: UI Components
- File/folder: `React/14_Machine_Coding_UI_Components`
- Function/code to work on: `Search with Filters`
- Original ask: Search with Filters. Key behavior: Faceted search, URL sync, chips. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Search with Filters. Key behavior: Faceted search, URL sync, chips. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Search with Filters. Key behavior: Faceted search, URL sync, chips. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.01 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Todo App with Filters`
- Original ask: Todo App with Filters. Key behavior: CRUD, filter/sort, localStorage. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Todo App with Filters. Key behavior: CRUD, filter/sort, localStorage. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Todo App with Filters. Key behavior: CRUD, filter/sort, localStorage. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.02 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Kanban Board`
- Original ask: Kanban Board. Key behavior: Drag-drop, columns, state. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Kanban Board. Key behavior: Drag-drop, columns, state. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Kanban Board. Key behavior: Drag-drop, columns, state. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.03 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `File Explorer / Tree View`
- Original ask: File Explorer / Tree View. Key behavior: Recursive, expand/collapse, lazy. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: File Explorer / Tree View. Key behavior: Recursive, expand/collapse, lazy. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: File Explorer / Tree View. Key behavior: Recursive, expand/collapse, lazy. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.04 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Infinite Scroll Feed`
- Original ask: Infinite Scroll Feed. Key behavior: Intersection observer, pagination. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Infinite Scroll Feed. Key behavior: Intersection observer, pagination. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Infinite Scroll Feed. Key behavior: Intersection observer, pagination. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.05 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Pagination with Filters`
- Original ask: Pagination with Filters. Key behavior: Page state, URL sync, prefetch. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Pagination with Filters. Key behavior: Page state, URL sync, prefetch. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Pagination with Filters. Key behavior: Page state, URL sync, prefetch. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.06 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Shopping Cart`
- Original ask: Shopping Cart. Key behavior: Add/remove, quantity, total, discount. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Shopping Cart. Key behavior: Add/remove, quantity, total, discount. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Shopping Cart. Key behavior: Add/remove, quantity, total, discount. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.07 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Comment Thread (Nested)`
- Original ask: Comment Thread (Nested). Key behavior: Recursive, reply, collapse. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Comment Thread (Nested). Key behavior: Recursive, reply, collapse. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Comment Thread (Nested). Key behavior: Recursive, reply, collapse. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.08 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Multi-step Form Wizard`
- Original ask: Multi-step Form Wizard. Key behavior: Navigation, validation, state persistence. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Multi-step Form Wizard. Key behavior: Navigation, validation, state persistence. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Multi-step Form Wizard. Key behavior: Navigation, validation, state persistence. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.09 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Real-time Chat UI`
- Original ask: Real-time Chat UI. Key behavior: Messages, scroll-to-bottom, typing indicator. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Real-time Chat UI. Key behavior: Messages, scroll-to-bottom, typing indicator. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Real-time Chat UI. Key behavior: Messages, scroll-to-bottom, typing indicator. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.10 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Poll/Voting App`
- Original ask: Poll/Voting App. Key behavior: Vote, results, real-time update. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Poll/Voting App. Key behavior: Vote, results, real-time update. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Poll/Voting App. Key behavior: Vote, results, real-time update. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.11 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Image Gallery with Lightbox`
- Original ask: Image Gallery with Lightbox. Key behavior: Grid, zoom, keyboard nav. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Image Gallery with Lightbox. Key behavior: Grid, zoom, keyboard nav. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Image Gallery with Lightbox. Key behavior: Grid, zoom, keyboard nav. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.12 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Notification Center`
- Original ask: Notification Center. Key behavior: Read/unread, mark all, filter by type. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Notification Center. Key behavior: Read/unread, mark all, filter by type. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Notification Center. Key behavior: Read/unread, mark all, filter by type. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.13 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Search with Recent + Suggestions`
- Original ask: Search with Recent + Suggestions. Key behavior: History, debounce, keyboard select. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Search with Recent + Suggestions. Key behavior: History, debounce, keyboard select. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Search with Recent + Suggestions. Key behavior: History, debounce, keyboard select. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.14 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Settings Page (Toggle Sections)`
- Original ask: Settings Page (Toggle Sections). Key behavior: Grouped settings, persist, reset. Difficulty: Easy.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Settings Page (Toggle Sections). Key behavior: Grouped settings, persist, reset. Difficulty: Easy.
- Real-life scenario: A user is on a real product screen that needs this behavior: Settings Page (Toggle Sections). Key behavior: Grouped settings, persist, reset. Difficulty: Easy. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.15 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Calendar / Event Scheduler`
- Original ask: Calendar / Event Scheduler. Key behavior: Month view, add/edit events, overlap. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Calendar / Event Scheduler. Key behavior: Month view, add/edit events, overlap. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Calendar / Event Scheduler. Key behavior: Month view, add/edit events, overlap. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.16 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Dashboard with Widgets`
- Original ask: Dashboard with Widgets. Key behavior: Grid layout, resize, drag. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Dashboard with Widgets. Key behavior: Grid layout, resize, drag. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Dashboard with Widgets. Key behavior: Grid layout, resize, drag. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.17 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Social Media Feed`
- Original ask: Social Media Feed. Key behavior: Like, comment, share, optimistic. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Social Media Feed. Key behavior: Like, comment, share, optimistic. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Social Media Feed. Key behavior: Like, comment, share, optimistic. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.18 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Music Player UI`
- Original ask: Music Player UI. Key behavior: Play/pause, progress, playlist. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Music Player UI. Key behavior: Play/pause, progress, playlist. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Music Player UI. Key behavior: Play/pause, progress, playlist. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.19 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Expense Tracker`
- Original ask: Expense Tracker. Key behavior: Add/categorize, chart, filter by date. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Expense Tracker. Key behavior: Add/categorize, chart, filter by date. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Expense Tracker. Key behavior: Add/categorize, chart, filter by date. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 15.20 - 15 - Machine Coding Round: App Features
- File/folder: `React/15_Machine_Coding_App_Features`
- Function/code to work on: `Quiz App with Timer`
- Original ask: Quiz App with Timer. Key behavior: Timer, score, review answers. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Quiz App with Timer. Key behavior: Timer, score, review answers. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Quiz App with Timer. Key behavior: Timer, score, review answers. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.01 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Debounce/Throttle Implementation`
- Original ask: Debounce/Throttle Implementation. Key behavior: Timer management, closure. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Debounce/Throttle Implementation. Key behavior: Timer management, closure. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Debounce/Throttle Implementation. Key behavior: Timer management, closure. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.02 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Undo/Redo System`
- Original ask: Undo/Redo System. Key behavior: Command pattern, stack. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Undo/Redo System. Key behavior: Command pattern, stack. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Undo/Redo System. Key behavior: Command pattern, stack. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.03 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Virtual Scroll (Fixed Height)`
- Original ask: Virtual Scroll (Fixed Height). Key behavior: Viewport math, scroll offset. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Virtual Scroll (Fixed Height). Key behavior: Viewport math, scroll offset. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Virtual Scroll (Fixed Height). Key behavior: Viewport math, scroll offset. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.04 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Virtual Scroll (Variable Height)`
- Original ask: Virtual Scroll (Variable Height). Key behavior: Height estimation, binary search. Difficulty: Expert.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Virtual Scroll (Variable Height). Key behavior: Height estimation, binary search. Difficulty: Expert.
- Real-life scenario: A user is on a real product screen that needs this behavior: Virtual Scroll (Variable Height). Key behavior: Height estimation, binary search. Difficulty: Expert. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.05 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Drag and Drop (Custom)`
- Original ask: Drag and Drop (Custom). Key behavior: Mouse events, coordinates, reorder. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Drag and Drop (Custom). Key behavior: Mouse events, coordinates, reorder. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Drag and Drop (Custom). Key behavior: Mouse events, coordinates, reorder. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.06 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Spreadsheet/Grid`
- Original ask: Spreadsheet/Grid. Key behavior: Cell reference, formula parser. Difficulty: Expert.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Spreadsheet/Grid. Key behavior: Cell reference, formula parser. Difficulty: Expert.
- Real-life scenario: A user is on a real product screen that needs this behavior: Spreadsheet/Grid. Key behavior: Cell reference, formula parser. Difficulty: Expert. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.07 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Traffic Light State Machine`
- Original ask: Traffic Light State Machine. Key behavior: State transitions, timers. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Traffic Light State Machine. Key behavior: State transitions, timers. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Traffic Light State Machine. Key behavior: State transitions, timers. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.08 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Promise Pool / Concurrency Limiter`
- Original ask: Promise Pool / Concurrency Limiter. Key behavior: Queue, promise resolution. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Promise Pool / Concurrency Limiter. Key behavior: Queue, promise resolution. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Promise Pool / Concurrency Limiter. Key behavior: Queue, promise resolution. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.09 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Event Emitter in React`
- Original ask: Event Emitter in React. Key behavior: Pub/sub, cleanup, hook integration. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Event Emitter in React. Key behavior: Pub/sub, cleanup, hook integration. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Event Emitter in React. Key behavior: Pub/sub, cleanup, hook integration. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.10 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Deep Object Comparison (React-specific)`
- Original ask: Deep Object Comparison (React-specific). Key behavior: Recursive equality, memo. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Deep Object Comparison (React-specific). Key behavior: Recursive equality, memo. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Deep Object Comparison (React-specific). Key behavior: Recursive equality, memo. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.11 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Retry with Backoff (Hook)`
- Original ask: Retry with Backoff (Hook). Key behavior: Exponential delay, max retries. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Retry with Backoff (Hook). Key behavior: Exponential delay, max retries. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Retry with Backoff (Hook). Key behavior: Exponential delay, max retries. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.12 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Flatten/Normalize Nested Data`
- Original ask: Flatten/Normalize Nested Data. Key behavior: Recursion, normalization. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Flatten/Normalize Nested Data. Key behavior: Recursion, normalization. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Flatten/Normalize Nested Data. Key behavior: Recursion, normalization. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.13 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Rate Limiter (UI)`
- Original ask: Rate Limiter (UI). Key behavior: Token bucket, time window. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Rate Limiter (UI). Key behavior: Token bucket, time window. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Rate Limiter (UI). Key behavior: Token bucket, time window. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.14 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Memoize Function (Generic)`
- Original ask: Memoize Function (Generic). Key behavior: Cache, key generation. Difficulty: Medium.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Memoize Function (Generic). Key behavior: Cache, key generation. Difficulty: Medium.
- Real-life scenario: A user is on a real product screen that needs this behavior: Memoize Function (Generic). Key behavior: Cache, key generation. Difficulty: Medium. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.15 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Build a Scheduler (setTimeout polyfill)`
- Original ask: Build a Scheduler (setTimeout polyfill). Key behavior: Priority queue, time management. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build a Scheduler (setTimeout polyfill). Key behavior: Priority queue, time management. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build a Scheduler (setTimeout polyfill). Key behavior: Priority queue, time management. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.16 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Implement React.memo from scratch`
- Original ask: Implement React.memo from scratch. Key behavior: Shallow compare, closure. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement React.memo from scratch. Key behavior: Shallow compare, closure. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement React.memo from scratch. Key behavior: Shallow compare, closure. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.17 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Dependency Graph (useEffect order)`
- Original ask: Dependency Graph (useEffect order). Key behavior: Topological sort, DAG. Difficulty: Expert.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Dependency Graph (useEffect order). Key behavior: Topological sort, DAG. Difficulty: Expert.
- Real-life scenario: A user is on a real product screen that needs this behavior: Dependency Graph (useEffect order). Key behavior: Topological sort, DAG. Difficulty: Expert. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.18 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Implement useSyncExternalStore (simplified)`
- Original ask: Implement useSyncExternalStore (simplified). Key behavior: Subscribe, snapshot, server. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Implement useSyncExternalStore (simplified). Key behavior: Subscribe, snapshot, server. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Implement useSyncExternalStore (simplified). Key behavior: Subscribe, snapshot, server. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.19 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `LRU Cache Hook`
- Original ask: LRU Cache Hook. Key behavior: Doubly-linked list, Map. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: LRU Cache Hook. Key behavior: Doubly-linked list, Map. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: LRU Cache Hook. Key behavior: Doubly-linked list, Map. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.

## Challenge 16.20 - 16 - Machine Coding Round: Logic Heavy
- File/folder: `React/16_Machine_Coding_Logic_Heavy`
- Function/code to work on: `Build useQuery from scratch`
- Original ask: Build useQuery from scratch. Key behavior: Cache, refetch, stale time. Difficulty: Hard.
- In simple words: Build the visible UI behavior described here and make it usable by a person: Build useQuery from scratch. Key behavior: Cache, refetch, stale time. Difficulty: Hard.
- Real-life scenario: A user is on a real product screen that needs this behavior: Build useQuery from scratch. Key behavior: Cache, refetch, stale time. Difficulty: Hard. The UI must feel working, not like a static mockup.
- What is going wrong: If this is only partly built, users may see broken state, missing interactions, poor keyboard behavior, no loading/error handling, or UI that cannot survive edge cases.
- What needs to be corrected: Build the component, hook, or screen behavior in the playground or a focused demo. Make it usable with mouse, keyboard where relevant, and realistic data states.
- Implementation checklist: Render the component, wire the state, handle user events, show empty/loading/error states when relevant, and check keyboard/focus behavior for interactive components.
- Expected output/result: A working React component, hook, or screen that renders correctly, responds to user actions, handles empty/loading/error states where relevant, and is explainable in an interview.
- How to test: Run `cd React/playground; npm install; npm run dev`, open the demo, and manually verify the main interaction, keyboard behavior if relevant, and edge cases. Add React Testing Library tests for important behavior when possible.
- You are done when: A user can perform the expected interaction in the playground and the UI still behaves correctly for edge cases.
